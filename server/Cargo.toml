[package]
name = "forage-server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features = ["rt-multi-thread", "net", "macros", "sync"] }
forage-core = { path = "../core" }
forage-network = { path = "../network" }
